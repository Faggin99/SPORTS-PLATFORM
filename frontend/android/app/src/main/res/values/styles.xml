<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema base -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Tema da activity principal (o app renderiza aqui) -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Background dark navy pra evitar flash branco entre splash e webview -->
        <item name="android:windowBackground">@color/splashBackground</item>
    </style>

    <!-- SplashScreen — Android 12+ usa windowSplashScreen* / <12 usa windowBackground.
         Sem esses items o Android mostra branco. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Background do splash (fundo dark navy) -->
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <!-- Ícone centralizado (o próprio launcher icon fica bonito) -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splashBackground</item>
        <!-- Ao terminar o splash, entra no tema principal -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <!-- Fallback pra Android <12: usa o drawable splash gerado (fundo dark + icon) -->
        <item name="android:windowBackground">@drawable/launch_splash</item>
    </style>

</resources>
